@model IEnumerable<AlbaCaZapada.Models.Payment>

<form method="get" asp-action="Index">
    <div class="container p-3">
        <div class="row pt-4">
            <div class="col-6">
                <h2 class="text-primary text-black-50">Taxa scolarizare pt:</h2>
                <h1>@ViewData["StudentName"]</h1>
            </div>
            <div class="col-6 text-right">
                <a class="btn btn-success fa-pull-right" asp-controller="Payment" asp-action="AddPayment" asp-route-Id="@ViewData["StudentId"]">Adauga plata</a>
            </div>
        </div>
        <br />
        @if (Model.Count() > 0)
        {
            <table class="table table-bordered table-striped" style="width:100%">
                <thead>
                    <tr>
                        <th>
                            Suma (RON)
                        </th>
                        <th>
                            Data platii
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var payment in Model)
                    {
                        <tr>
                            <td width="45%">@payment.Amount</td>
                            <td width="45%">@payment.PaymentDate.ToString("dd/MM/yyyy")</td>
                            <td width="10%">
                                <a class="btn btn-danger" asp-controller="Payment" asp-action="DeletePayment" asp-route-Id="@payment.Id">Sterge</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <p>Nu exista nici o plate pt acest elev!</p>
        }
        <a class="btn btn-success" asp-controller="Student" asp-action="Index">Inapoi</a>
    </div>
</form>

@section Scripts
{
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}
